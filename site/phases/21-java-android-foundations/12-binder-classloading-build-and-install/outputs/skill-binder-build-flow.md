# Binder 与构建流程检查卡

## 使用时机

排查跨进程大事务、动态加载、构建产物或安装行为时使用。

## 检查步骤

1. Binder 事务只传轻量数据；大数据改 URI、文件描述符或分块协议。
2. 安装应用代码用 `PathClassLoader`；受控可信插件才用 `DexClassLoader`。
3. 构建顺序核对 AAPT2、编译、D8、R8、打包、签名和 zipalign。
4. 安装问题查看 Package Manager 的解析、权限、组件和数据目录状态。
5. 不把动态加载视为规避模块依赖的通用方案。
