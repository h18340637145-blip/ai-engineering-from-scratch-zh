# 列表与图片缓存优化卡

## 使用时机

列表滚动掉帧、嵌套列表卡顿、图片闪烁或内存飙升时使用。

## 检查步骤

1. 用局部更新或 `DiffUtil` 代替整表 `notifyDataSetChanged()`。
2. 嵌套横向列表复用 `RecycledViewPool`，在语义允许时声明固定尺寸。
3. 按 memory → disk → network 检查图片读取路径。
4. 使用目标尺寸采样解码，避免原图直接进入列表。
5. 不将软引用作为主缓存；通过测量确认绑定、解码、GC 或布局热点。
