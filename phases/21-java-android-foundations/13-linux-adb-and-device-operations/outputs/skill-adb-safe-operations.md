# 安全 ADB 操作卡

## 使用时机

需要在真实或模拟设备上收集证据、部署调试或复现 UI 行为时使用。

## 检查步骤

1. 多设备时总是先 `adb devices`，随后用 `-s <serial>` 固定目标。
2. 包与组件用 `pm`、`am`；系统状态用 `dumpsys`；时间线用 Logcat。
3. root、remount、SELinux 切换只在可恢复的受控设备上使用。
4. 先打印绝对目标路径，再执行任何删除或覆盖操作。
5. 禁止把 `rm -rf *` 用于不确定目录。
