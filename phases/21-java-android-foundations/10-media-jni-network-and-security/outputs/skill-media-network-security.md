# 多媒体、JNI 与安全选型卡

## 使用时机

设计相机/音视频管线、Native 桥接、网络协议或加密方案时使用。

## 检查步骤

1. 媒体管线保留采集、编码、时间戳和封装阶段。
2. 只有复用 Native 库或性能敏感路径才引入 JNI。
3. 可靠有序流选 TCP；低延迟可容忍丢包选 UDP。
4. 大数据用 AES；签名和密钥协商用 RSA/ECC；密码存储用 KDF。
5. 不把 MD5、SHA-1 作为新安全设计的核心算法。
