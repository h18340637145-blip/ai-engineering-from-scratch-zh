# Java 类型边界检查卡

## 使用时机

设计 Java API、审查泛型参数或评估字符串拼接代码时使用。

## 检查清单

1. 有共享实例状态或半成品实现时，选择抽象类。
2. 只需描述能力且可能多实现时，选择接口。
3. 少量文本使用 `String`；单线程频繁拼接用 `StringBuilder`；共享缓冲区才用 `StringBuffer`。
4. `? extends T` 只读，`? super T` 写入 T。
5. 不让内部类、匿名回调无意持有页面或外部实例。
