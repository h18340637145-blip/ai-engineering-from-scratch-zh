# Java 运行时安全检查卡

## 使用时机

涉及反射、字节反序列化、内存回收或资源生命周期时使用。

## 检查清单

1. 优先常规接口调用；反射仅用于框架、插件或注解处理等明确场景。
2. 不反序列化不可信字节流，优先显式 JSON 或 Protobuf 格式。
3. 诊断泄漏时从线程栈、静态字段和 JNI 引用追踪 GC Roots。
4. 不用 `finalize()` 管理文件、Cursor、流或网络连接。
5. 软引用缓存不是可靠的主缓存；弱引用不保证对象长期存在。
